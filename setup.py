from setuptools import setup, find_packages
setup(name='special basoon', vertion='0.1.0', author='Harel Etgar', description='project for course advanced system design', packages=find_packages(), install_requires=['sphinx', 'click', 'flask', 'Pillow'], tests_require=['pytest'])
